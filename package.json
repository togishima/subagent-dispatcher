{
  "name": "jev-dispatch",
  "version": "0.2.0",
  "private": true,
  "description": "Jev-powered subagent scheduler experiment platform for Claude Code",
  "license": "MIT",
  "type": "module",
  "engines": { "node": ">=22.5.0" },
  "bin": { "jev-dispatch": "./bin/jev-dispatch" },
  "scripts": {
    "test": "node --test --experimental-sqlite \"test/**/*.test.mjs\"",
    "ui": "node --experimental-sqlite bin/jev-dispatch ui",
    "doctor": "node --experimental-sqlite bin/jev-dispatch doctor"
  },
  "dependencies": {}
}
